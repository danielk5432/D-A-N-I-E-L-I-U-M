using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Enemy_Fire : MonoBehaviour {
	/*
	Enemy_Bullet_Pool EBullet;



	// Use this for initialization
	void Start () {
		EBullet = GameObject.Find("Level Control").GetComponent<Enemy_Bullet_Pool>();
	}
	
	// Update is called once per frame
	public void EFire1 () {
		GameObject proj = EBullet.EPooled_Bullet.Dequeue;
		proj.SetActive(true);
		proj.transform.position = transform.position;
	}*/
}